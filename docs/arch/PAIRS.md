# Conversion pairs — every combination, one row each

Generated by `scripts/gen-pairs.mjs` from the declared interop contracts. Do not
edit by hand.

420 ordered pairs across 21 concrete formats.
Find your source format's section, then your target row. For what the columns mean
and how to supply evidence, see [CONVERT.md](./CONVERT.md).

Rows describe a lock that does not use the target-exclusive features listed in
[what you lose](./CONVERT.md#what-you-lose); a lock that does adds that loss on top.
"Add to the call" names the option that repairs the gap — everything under
"What the target drops" is what the target format does not carry in this pair.

**Dropped is not the same as unrecoverable.** Where the target does have a carrier
for a field, enrichment can refill it — cross-family tarball payload metadata is the
common case, restored authoritatively from `sources.packuments` or
`sources.manifests`. Where it has none, the field is gone whatever you supply: into
`npm-1`, for instance, the entry schema holds only version, resolved and integrity.
Two entries are never losses at all — `workspace-rekey` means the target re-keys
node identity and preserves the graph, and `compressionLevel` is a producer-faithful
repair every pinned Berry release performs itself. Only ❌ rows and the
reasons spelled out in the cell are categorically unrecoverable; for the per-pair
argument see `LossEntry.rationale` in `src/test/interop/_matrix.ts`.

| | Meaning | Pairs |
| :-: | --- | --: |
| ✅ | Nothing to supply. Anything in the last column is something the target manager would drop itself. | 73 |
| ⚠️ | It converts, but evidence must be supplied or a real feature drops. Read the row. | 276 |
| ❌ | Refused before parsing. No option makes it work. | 71 |

## Why a row can be stricter than the interop matrix

Diffing this file against `_matrix.ts` will show requirements the matrix does not
declare. That is deliberate, not drift. The matrix declares what its **fixtures
prove**; a format's structural requirements are wider. Measured on the contracts
this file was generated from:

- **98 of 98** non-berry → yarn-berry pairs declare no artifact
  requirement, although a Berry checksum covers a zip only Yarn produces and must be
  recomputed from bytes. Every single one — so this is a systematic blind spot in the matrix, not a
  tally of exceptions, and the matrix is the thing to fix eventually.
- **7 of 16** yarn-classic-source pairs declare nothing at
  all, although a `yarn.lock` carries no project root, omits independent workspace
  members, and cannot classify `dev` or `peer`.

So the generator applies a capability overlay of four rules on top of the declared
contracts, each traceable in `scripts/gen-pairs.mjs` to a measured fact. Where the
two disagree, these rows are the stricter and the more truthful.

## From `npm-1`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-2` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-3` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-4` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `bun-text` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `npm-2`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `tarballs`, `workspace`) | `strict: false` | unkeyable dependency edges; tarball payload metadata; workspace members | ⚠️ |
| `npm-3` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-4` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `npm-3`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `tarballs`, `workspace`) | `strict: false` | unkeyable dependency edges; tarball payload metadata; workspace members | ⚠️ |
| `npm-2` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-4` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `npm-4`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `tarballs`, `workspace`) | `strict: false` | unkeyable dependency edges; tarball payload metadata; workspace members | ⚠️ |
| `npm-2` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-3` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`, `workspace-root-version`, `edges`, `edge-kinds`, `workspace-metadata`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the root workspace version; unkeyable dependency edges; edge kinds; workspace metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`resolved-url`, `tarballs`, `workspace-root-version`, `edges`, `edge-kinds`, `workspace-metadata`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the root workspace version; unkeyable dependency edges; edge kinds; workspace metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-classic`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `npm-2` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `npm-3` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `npm-4` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`multi-spec-collapsed`, `workspace-rekey`) | `sources.manifests`<br>`sources.manifests` (root + members)<br>`sources.artifacts` (Berry checksum)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; merged descriptor sets; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `pnpm-v6` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `pnpm-v9` | `YARN_CLASSIC_NO_MANIFESTS` | `sources.manifests` (root + members) | `peer` edges — a `yarn.lock` cannot record them | ⚠️ |
| `bun-text` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`resolved-url`) | `sources.manifests` (root + members)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `bun-text-v2` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`resolved-url`) | `sources.manifests` (root + members)<br>`strict: false` | `peer` edges — a `yarn.lock` cannot record them; resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v4`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v5`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v6`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v7`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v8`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v9`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v10` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `yarn-berry-v10`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `resolved-url`, `tarballs`, `patch`) | `strict: false` | unkeyable dependency edges; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata; the patch recipe | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`resolved-url`) | `strict: false` | resolution type — degrades to `unknown`; the raw locator survives as attribution | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`peer-virt`, `patch`, `sentinel-collapsed`, `virtual`, `workspace-metadata`, `conditions`, `compressionLevel`, `cacheKey`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; peer virtualization; the patch recipe; unresolved-sentinel patch slots; virtual node identities; workspace metadata; the `conditions` block; the `compressionLevel` subkey; the checksum's cache-key domain; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `PROJECTION_LOSS` (`conditions`, `compressionLevel`) | `strict: false` | the `conditions` block; the `compressionLevel` subkey | ⚠️ |
| `yarn-berry-v5` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v6` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v7` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v8` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `yarn-berry-v9` | `PROJECTION_LOSS` (`compressionLevel`) | `strict: false` | the `compressionLevel` subkey | ✅ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`workspace-rekey`, `resolved-url`, `tarballs`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `pnpm-v5`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `peer-virt`, `patch`, `tarballs`) | `strict: false` | unkeyable dependency edges; peer virtualization; the patch recipe; tarball payload metadata | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `peer-virt`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; peer virtualization; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v6` | `PROJECTION_LOSS` (`tarballs`) | `strict: false` | tarball payload metadata | ⚠️ |
| `pnpm-v9` | `PROJECTION_LOSS` (`tarballs`) | `strict: false` | tarball payload metadata | ⚠️ |
| `bun-text` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `pnpm-v6`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `peer-virt`, `patch`, `tarballs`) | `strict: false` | unkeyable dependency edges; peer virtualization; the patch recipe; tarball payload metadata | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `peer-virt`, `patch`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; peer virtualization; the patch recipe; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`tarballs`, `patch`) | `strict: false` | tarball payload metadata; the patch recipe | ⚠️ |
| `pnpm-v9` | — | nothing; the lock alone is enough | — | ✅ |
| `bun-text` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `pnpm-v9`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`, `peer-virt`, `patch`, `tarballs`) | `strict: false` | unkeyable dependency edges; peer virtualization; the patch recipe; tarball payload metadata | ⚠️ |
| `npm-2` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-3` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `npm-4` | `PROJECTION_LOSS` (`peer-virt`) | `strict: false` | peer virtualization | ⚠️ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`, `peer-virt`, `patch`, `tarballs`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata; peer virtualization; the patch recipe; tarball payload metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`peer-virt`, `tarballs`, `workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | peer virtualization; tarball payload metadata; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`tarballs`, `patch`) | `strict: false` | tarball payload metadata; the patch recipe | ⚠️ |
| `pnpm-v6` | — | nothing; the lock alone is enough | — | ✅ |
| `bun-text` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `bun-text-v2` | `PROJECTION_LOSS` (`peer-virt`, `resolved-url`, `tarballs`) | `strict: false` | peer virtualization; resolution type — degrades to `unknown`; the raw locator survives as attribution; tarball payload metadata | ⚠️ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `bun-text`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`) | `strict: false` | unkeyable dependency edges | ⚠️ |
| `npm-2` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-3` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-4` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `bun-text-v2` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `bun-text-v2`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `PROJECTION_LOSS` (`edges`) | `strict: false` | unkeyable dependency edges | ⚠️ |
| `npm-2` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-3` | — | nothing; the lock alone is enough | — | ✅ |
| `npm-4` | — | nothing; the lock alone is enough | — | ✅ |
| `yarn-classic` | `YARN_CLASSIC_NO_MANIFESTS`<br>`PROJECTION_LOSS` (`workspace-metadata`) | `sources.manifests` (for `dev`)<br>`strict: false` | `peer` edges — no field in the target; workspace metadata | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`PROJECTION_LOSS` (`workspace-rekey`) | `sources.artifacts` (Berry checksum)<br>`strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v6` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `pnpm-v9` | `PROJECTION_LOSS` (`workspace-rekey`) | `strict: false` | workspace node ids — rekeyed to the target's scheme, graph preserved | ✅ |
| `bun-text` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v2` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v3` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v4` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks target-specific producer-certified synthesis of Deno native npm ids, peer suffixes, integrity, and root specifier bindings | ❌ |

## From `deno-v2`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-3` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `yarn-classic` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>`sources.manifests` (for `dev`)<br>the sibling `deno.json`<br>`strict: false` | `peer` edges — no field in the target; JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `bun-text` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `bun-text-v2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `deno-v3` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v4` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks complete v5 package metadata and proof that existing dependency, optional-dependency, and peer edges were correctly reclassified | ❌ |

## From `deno-v3`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-3` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `yarn-classic` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>`sources.manifests` (for `dev`)<br>the sibling `deno.json`<br>`strict: false` | `peer` edges — no field in the target; JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `bun-text` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `bun-text-v2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `deno-v2` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v4` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks complete v5 package metadata and proof that existing dependency, optional-dependency, and peer edges were correctly reclassified | ❌ |

## From `deno-v4`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-3` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `yarn-classic` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>`sources.manifests` (for `dev`)<br>the sibling `deno.json`<br>`strict: false` | `peer` edges — no field in the target; JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `bun-text` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `bun-text-v2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `deno-v2` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v3` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v5` | `CAPABILITY_LACK` | nothing — the pair is refused before parsing | lacks complete v5 package metadata and proof that existing dependency, optional-dependency, and peer edges were correctly reclassified | ❌ |

## From `deno-v5`

| To | Expected diagnostic | Add to the call | What the target drops | |
| --- | --- | --- | --- | :-: |
| `npm-1` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-3` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `npm-4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization | ⚠️ |
| `yarn-classic` | `ENRICH_REQUIRED`<br>`YARN_CLASSIC_NO_MANIFESTS`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>`sources.manifests` (for `dev`)<br>the sibling `deno.json`<br>`strict: false` | `peer` edges — no field in the target; JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `yarn-berry-v4` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v7` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v8` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `yarn-berry-v10` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-rekey`) | `sources.manifests`<br>`sources.artifacts` (Berry checksum)<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v5` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v6` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `pnpm-v9` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `workspace-rekey`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; workspace node ids — rekeyed to the target's scheme, graph preserved | ⚠️ |
| `bun-text` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `bun-text-v2` | `ENRICH_REQUIRED`<br>`DENO_MANIFEST_REQUIRED`<br>`PROJECTION_LOSS` (`deno-jsr`, `deno-remote`, `peer-virt`, `workspace-root-version`) | `sources.manifests`<br>the sibling `deno.json`<br>`strict: false` | JSR packages; remote-URL modules; peer virtualization; the root workspace version | ⚠️ |
| `deno-v2` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v3` | — | nothing; the lock alone is enough | — | ✅ |
| `deno-v4` | — | nothing; the lock alone is enough | — | ✅ |
